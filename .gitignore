node_modules/
.env
.env.*
# except the two that are meant to ship: a template, and the public
# addresses the deployed frontend is built with
!.env.example
!web/.env.production
.next/
pgdata/
*.log

# Local working documents — never shipped
trustgrade-prd.md
TrustGrade-brief.md
NOTES.md

# Scratch files from review runs, never part of the suite
server/tests/_*
_*.mjs
.vercel
